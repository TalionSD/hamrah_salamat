enum AppState {
  stable,
  loading,
  success,
  error,
}

enum Gender {
  male,
  female,
}

enum ActivityLevel {
  noActivity,
  lowActivity,
  moderateActivity,
  highActivity,
}

enum StatusOfDay {
  hasPassed,
  active,
  notArrived,
}

enum RootScreenItems {
  home,
  targeting,
  dietPlaning,
  profile,
}

enum TypeOfDietPlaning {
  template,
  custom,
  ai,
}

enum SnackBarType {
  error,
  success,
}

enum IntroductionScreens {
  login,
  dietPlanning,
  targeting,
  intro,
}
